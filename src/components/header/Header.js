import NavbarContainer from '../navbar/NavbarContainer'
import './Header.css'

const Header = () => (
  <header className="header">
    <h1 className="header__title">Soccer Stat</h1>
    <NavbarContainer />
  </header>
)

export default Header
